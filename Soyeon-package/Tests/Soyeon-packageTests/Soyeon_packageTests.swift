import XCTest
@testable import Soyeon_package

final class Soyeon_packageTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Soyeon_package().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
